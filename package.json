{
  "name": "w3s-usdckit-contracts",
  "version": "1.0.0",
  "description": "w3s-usdckit-contracts",
  "scripts": {
    "build": "forge build",
    "lint": "solhint -w 0 -c .solhint-src.json './src/**/*.sol' && solhint -w 0 -c .solhint-test.json './test/**/*.sol'",
    "test": "forge test -vv",
    "coverage": "forge coverage",
    "format:check": "forge fmt --check",
    "format:write": "forge fmt"
  },
  "author": "circle",
  "license": "GPL-3.0-or-later",
  "directories": {
    "test": "test"
  },
  "keywords": [],
  "repository": "git@github.com:circlefin/w3s-usdckit-contracts.git",
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  },
  "devDependencies": {
    "solhint": "^5.0.5"
  }
}
